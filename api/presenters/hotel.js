export function hotelResponse({
  name,
  contactPhone,
  contactEmail,
  address,
  hotelId,
  createdAt,
  updatedAt,
}) {
  return {
    name,
    contactPhone,
    contactEmail,
    address,
    hotelId,
    createdAt,
    updatedAt,
  };
}
