// app/request-callback/layout.jsx

export const metadata = {
  title: 'Request Callback',
};

export default function RequestCallbackLayout({ children }) {
  return <div className="m-0 bg-transparent">{children}</div>;
}
